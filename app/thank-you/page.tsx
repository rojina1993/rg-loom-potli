"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { Logo } from "@/components/Logo";
import { money } from "@/lib/product";
type Order = { orderId: string; productName: string; quantity: number; totalPrice: number };
export default function ThankYou() { const [order, setOrder] = useState<Order | null>(null); useEffect(() => { const saved = sessionStorage.getItem("rg-loom-order"); if (saved) setOrder(JSON.parse(saved)); }, []); return <main className="grid min-h-screen place-items-center bg-pistachio px-5"><div className="w-full max-w-xl rounded-[2.5rem] bg-white p-8 text-center shadow-xl sm:p-12"><Logo /><div className="mx-auto mt-8 grid h-16 w-16 place-items-center rounded-full bg-pistachio text-3xl">✓</div><h1 className="mt-5 font-serif text-4xl text-plum">Thank you for your order!</h1><p className="mt-4 leading-7 text-stone-600">Our sales representative will call you soon to confirm your order.</p>{order && <div className="mt-8 rounded-2xl bg-[#fff8f1] p-5 text-left text-sm"><div className="flex justify-between"><span>Order ID</span><b>{order.orderId}</b></div><div className="mt-3 flex justify-between"><span>Product</span><b>{order.productName}</b></div><div className="mt-3 flex justify-between"><span>Quantity</span><b>{order.quantity}</b></div><div className="mt-3 flex justify-between text-plum"><span>Total</span><b>{money(order.totalPrice)}</b></div><div className="mt-3 flex justify-between"><span>Payment</span><b>Cash On Delivery</b></div></div>}<Link href="/" className="mt-8 inline-block rounded-full bg-plum px-7 py-4 font-bold text-white">Back to Home</Link></div></main>; }
