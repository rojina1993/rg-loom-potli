"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { money, product } from "@/lib/product";
export function ProductChooser() { const router = useRouter(); const [gift, setGift] = useState(false); const [qty, setQty] = useState(1); const price = gift ? product.giftPrice : product.regularPrice; const go = () => router.push(`/checkout?plan=${gift ? "gift" : "classic"}&qty=${qty}`); return <div className="rounded-[2rem] bg-white p-6 shadow-sm ring-1 ring-plum/10"><p className="text-sm font-bold uppercase tracking-widest text-plum">Choose your story</p><div className="mt-4 grid grid-cols-2 gap-2 rounded-2xl bg-pistachio p-1"><button onClick={() => setGift(false)} className={`rounded-xl px-3 py-3 text-sm font-bold ${!gift ? "bg-white text-plum shadow" : ""}`}>Potli only</button><button onClick={() => setGift(true)} className={`rounded-xl px-3 py-3 text-sm font-bold ${gift ? "bg-white text-plum shadow" : ""}`}>Gift package</button></div><p className="mt-4 text-sm text-stone-600">{gift ? "Premium packaging + your personalised handwritten note." : "A one-of-a-kind handmade potli bag."}</p><div className="mt-5 flex items-center justify-between"><span className="font-serif text-3xl text-plum">{money(price)}</span><div className="flex items-center gap-3 rounded-full border border-plum/15 px-2 py-1"><button aria-label="Decrease quantity" onClick={() => setQty(Math.max(1, qty - 1))} className="h-8 w-8 rounded-full text-xl">−</button><span className="w-4 text-center font-bold">{qty}</span><button aria-label="Increase quantity" onClick={() => setQty(qty + 1)} className="h-8 w-8 rounded-full bg-pistachio text-xl">+</button></div></div><div className="mt-3 flex justify-between border-t border-plum/10 pt-3 font-bold"><span>Total</span><span>{money(price * qty)}</span></div><button onClick={go} className="mt-5 w-full rounded-full bg-plum px-5 py-4 font-bold text-white transition hover:bg-[#48102c]">Purchase Now</button></div>; }
