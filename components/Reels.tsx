"use client";
import { useState } from "react";
const reels = ["7657473863206358292", "7659300179585158421"];
export function Reels() { const [playing, setPlaying] = useState<number | null>(null); return <section className="bg-pistachio px-5 py-20"><div className="mx-auto max-w-6xl text-center"><p className="text-sm font-bold uppercase tracking-[.18em] text-plum">In motion</p><h2 className="mt-3 font-serif text-4xl text-plum">A closer look at the craft</h2><div className="mt-10 flex flex-wrap justify-center gap-8">{reels.map((id, i) => <div key={id} className="w-[260px] rounded-[2.8rem] border-[10px] border-plum bg-plum p-1 shadow-xl"><div className="relative aspect-[9/16] overflow-hidden rounded-[2rem] bg-black">{playing === i ? <iframe src={`https://www.tiktok.com/embed/v2/${id}`} title={`RG LOOM reel ${i + 1}`} className="h-full w-full" allow="fullscreen" /> : <button onClick={() => setPlaying(i)} className="grid h-full w-full place-items-center bg-gradient-to-br from-berry to-plum text-center text-white"><span className="rounded-full border border-white/60 px-5 py-3 text-sm font-bold">Play reel</span></button>}</div></div>)}</div></div></section>; }
